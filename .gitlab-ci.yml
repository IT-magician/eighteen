stages:          # List of stages for jobs, and their order of execution
  - libs
  # - build
  # - deploy


npm:
  stage: libs
  # only:
  #   - dev
  include:
    - local: './react-project'
  cache:
    paths:
      - react-project/node_modules
  script:
    - npm install
