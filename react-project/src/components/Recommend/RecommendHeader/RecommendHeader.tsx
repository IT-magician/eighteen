import React from "react";
import styled from "styled-components";

const RecommendHeader = (): JSX.Element => {
  return <StyledHeader></StyledHeader>;
};

const StyledHeader = styled.header``;

export default RecommendHeader;
