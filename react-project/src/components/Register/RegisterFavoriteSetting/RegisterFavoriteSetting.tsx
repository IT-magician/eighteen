import React from "react";
import styled from "styled-components";

const RegisterFavoriteSetting = (): JSX.Element => {
  return <StyledDiv></StyledDiv>;
};

const StyledDiv = styled.div``;

export default RegisterFavoriteSetting;
