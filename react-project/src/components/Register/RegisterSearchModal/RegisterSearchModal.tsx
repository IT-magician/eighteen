import React from "react";
import styled from "styled-components";

const RegisterSearchModal = (): JSX.Element => {
  return <StyledDiv></StyledDiv>;
};

const StyledDiv = styled.div``;

export default RegisterSearchModal;
