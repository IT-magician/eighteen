import React from "react";
import styled from "styled-components";

/**
 * 로그인 화면
 */
const Login = (): JSX.Element => {
  return <StyledDiv>login</StyledDiv>;
};

const StyledDiv = styled.div``;

export default Login;
