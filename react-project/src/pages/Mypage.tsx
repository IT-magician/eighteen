import React from "react";
import styled from "styled-components";

/**
 * 마이페이지
 */
const Mypage = (): JSX.Element => {
  return <StyledDiv>mypage</StyledDiv>;
};

const StyledDiv = styled.div``;

export default Mypage;
