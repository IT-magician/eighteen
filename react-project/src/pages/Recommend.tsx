import React from "react";
import styled from "styled-components";

/**
 * 추천 화면
 */
const Recommend = (): JSX.Element => {
  return <StyledDiv>recommend</StyledDiv>;
};

const StyledDiv = styled.div``;

export default Recommend;
