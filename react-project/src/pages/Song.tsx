import React from "react";
import styled from "styled-components";

/**
 * 전체 곡 목록
 */
const Song = (): JSX.Element => {
  return <StyledDiv>Song</StyledDiv>;
};

const StyledDiv = styled.div``;

export default Song;
